#include <cstdio>
#include <fstream>
#include <memory>
#include <stdexcept>
#include <string>
#include <array>

using namespace std;

int executeWithCode(const char* cmd);
int liveLog(const char* cmd);